# Local IDE / tooling
.idea/
.claude/
.vscode/
.playwright-mcp/

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / frontend
node_modules/
frontend/dist/

# Guards against accidentally running `npm install` from the repo root
# instead of inside frontend/ (happened once, produced a stray empty
# lockfile) -- anchored to root only, doesn't affect frontend/'s real one.
/package-lock.json
/package.json

# Env files
.env
.env.*
!.env.example

# Docker
**/*.log

# OS
.DS_Store
Thumbs.db
